# Built binaries
/bin/
/auditor
*.test
*.exe

# Coverage / profiling
coverage.out
coverage.html
*.prof

# Local config that might contain credentials
/auditor.yaml
/.auditor.yaml

# Scratch area for machine-local test data — snapshots pulled from real
# tenancies while developing. Never commit anything from here: an audit
# snapshot is a complete map of someone's infrastructure, including private
# addresses, hostnames and account identifiers.
/.local/

# IDE / OS junk
.idea/
.vscode/
.DS_Store
