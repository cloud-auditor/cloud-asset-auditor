# Build inputs/outputs. The *export* is not ignored here — it is copied to
# internal/server/webui/ and committed, so `go build` works without Node.
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Written by `next dev` on every start (see the note inside them). Generated,
# not authored — the project's own guidance lives in the repo-root CLAUDE.md.
AGENTS.md
CLAUDE.md
